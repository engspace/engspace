{
    "server": {
        "port": 3000
    },
    "db": {
        "uri": "postgresql://postgres:postgres@localhost",
        "name": "engspace",
        "options": {},
        "formatDb": false
    },
    "sessionSigningKeys": ["some key", "some other key"]
}
