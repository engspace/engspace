{
    "db": {
        "uri": "postgresql://postgres:postgres@localhost",
        "name": "engspace_server_db_test",
        "options": {
            "idleTimeout": 1000
        },
        "formatDb": true
    }
}
