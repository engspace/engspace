{
    "db": {
        "uri": "postgresql://postgres:postgres@localhost",
        "name": "engspace_test",
        "options": {
            "idleTimeout": 1000
        },
        "formatDb": true
    }
}
